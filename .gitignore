# Swamp runtime — generated by swamp CLI, never committed
.swamp/

# Swamp CLI skills scaffolded into .claude/ by `swamp repo init` —
# they're developer tooling, not part of this extension. Our own
# extension skill at .claude/skills/nats/ stays trackable.
.claude/skills/swamp-*/
